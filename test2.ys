import fs from "fs";

import Lexer, { Token } from "./Lexer";

const lexer = new Lexer("./l.tokens");
const program = fs.readFileSync("./test2.ys").toString("utf-8");

const tokens = lexer.lex(program);
console.log(tokens);

